# AI Studio Preview Mode:
# No environment variables are required. The preview runs directly with rich local mock data.
# For optional VS Code local backend setup with MongoDB & JWT, see /server/README.md.
